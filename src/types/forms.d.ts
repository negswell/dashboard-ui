/** Select dropdown Props */
interface ISelectPeers {
  label: string;
  value: string;
}

/** Peer Response */
interface GetPeerResponse {
  uid: string;
  name: string;
}

/** Peer  Form Values */
interface IPeerFormValues {
  uid: string;
  review: string;
}
